{
  "extends": "@quasar/app-vite/tsconfig-preset",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "*": [
        "./node_modules/*",
        "./src/*"
        // set path `@/*` as alias of `src/*`
      ]
    }
  }
}
